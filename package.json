{
  "name": "victoria",
  "version": "0.0.1",
  "description": "Simple object-hash relational mapper library for Redis, targeting Node.js 6 and above.",
  "main": "./index.js",
  "scripts": {
    "test": "tap test/{api,model,validators}.js",
    "coverage": "npm run test -- --cov --coverage-report=lcov",
    "cloc": "cloc lib test"
  },
  "dependencies": {
    "redis": "^2.6.0-2"
  },
  "engines": {
    "node": ">=6"
  },
  "devDependencies": {
    "co": "^4.6.0",
    "only": "0.0.2",
    "tap": "schwarzkopfb/node-tap"
  }
}
