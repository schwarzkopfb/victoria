{
  "name": "victoria",
  "version": "0.0.3",
  "description": "Simple object-hash relational mapper library for Redis, targeting Node.js 6 and above.",
  "main": "./index.js",
  "scripts": {
    "test": "tap test/*.js",
    "coverage": "npm run test -- --cov --coverage-report=lcov",
    "test-cleanup": "node ./test/cleanup --interactive",
    "cloc": "cloc lib test"
  },
  "dependencies": {
    "escape-html": "^1.0.3",
    "redis": "^2.6.0-2"
  },
  "engines": {
    "node": ">=6"
  },
  "devDependencies": {
    "co": "^4.6.0",
    "colors": "^1.1.2",
    "coveralls": "^2.11.9",
    "only": "0.0.2",
    "tap": "schwarzkopfb/node-tap"
  }
}
